"""Re-export every model module here so Base.metadata sees them all
before alembic autogenerate runs.

When you add a model file (e.g., user.py), import it here:

    from app.db.models import user  # noqa: F401
"""
